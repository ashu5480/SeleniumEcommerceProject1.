pipeline {
    agent any
tools {
        jdk 'JDK211'
        maven 'Maven'
    }
    stages {
        stage('Run Tests') {
            steps {
                bat 'mvn clean test'
            }
        }
    }
}
